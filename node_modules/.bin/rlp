#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../foundry-primitives/node_modules/rlp/bin/rlp" "$@"
  ret=$?
else 
  node  "$basedir/../foundry-primitives/node_modules/rlp/bin/rlp" "$@"
  ret=$?
fi
exit $ret
